import { ClickSliderDirective } from './click-slider.directive';

describe('ClickSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickSliderDirective();
    expect(directive).toBeTruthy();
  });
});
